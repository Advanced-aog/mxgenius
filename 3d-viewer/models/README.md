Place your .glb, .gltf, .fbx, or .obj model files here.

The GitHub Actions workflow will auto-scan this folder and generate models.json on deploy.
Just drop files and push — no manifest editing required.

File naming tip (MilVerse convention):
  SM_Truck_Base_v1.fbx → displays as "Truck Base v1"
